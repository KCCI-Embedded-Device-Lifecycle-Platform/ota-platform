services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: ota
      POSTGRES_USER: ota
      POSTGRES_PASSWORD: ota_dev_password
    ports:
      - "5432:5432"
    volumes:
      - ota-postgres-data:/var/lib/postgresql

volumes:
  ota-postgres-data: